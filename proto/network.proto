syntax = "proto3";

package network;

service Network {
    rpc CmdRequest(Request) returns (Response){}
}

message Request {
    string type = 1;
    string target = 2;
    string options = 3;
}

message Response {
    string json = 1;
}